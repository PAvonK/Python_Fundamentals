# String Operatios - 2000 (slicing / Len)

len('eric') # output 4

print('eric'[1]) # output r, because indexing starts at 0
print('eric'[0]) # output e
print('eric'[0:2]) # output er becasue it goes from 0 up to but on including 2
print('eric'[-1]) # output c because -1 starts at the end.
print('eric'[-3]) #output r because it is third from end
name = 'eric'
print(name[0]) # Because I bound eric to name if we index name we get the
# of 0 ini eric

print('eric'[0:2]) # output er becasue it goes from 0 up to but on including 2
print('eric'[:3]) # output eri because :3 is essentiall 0 - 3
print('eric'[1:]) # output ric because it returns all elements starting at 1
print('eric'[:]) # makes copy of the entire string

